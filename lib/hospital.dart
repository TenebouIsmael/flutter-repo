import 'package:flutter/material.dart';

class HospitalTiles extends StatelessWidget {
  const HospitalTiles({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
